# 图谱要素数据结构

本文档说明 `卷宗_原型_v4.html` 中图谱每个要素的数据结构。

## 总体结构

图谱由 4 层数据组成：

```js
{
  NODES,              // 要素节点的静态布局与媒体
  STEPS,              // 每个演示阶段下，各要素的状态摘要
  DETAIL_BY_STEP,     // 每个阶段下，各要素展开卡片的证据/缺口/矛盾明细
  MATERIALS           // 证据/文书/视频材料索引，用于打开材料抽屉
}
```

核心代码位置：

- `NODES`: `卷宗_原型_v4.html:895`
- `STEPS`: `卷宗_原型_v4.html:906`
- `MATERIALS`: `卷宗_原型_v4.html:1051`
- `DETAIL_BY_STEP`: `卷宗_原型_v4.html:1656`
- `renderDetail`: `卷宗_原型_v4.html:2121`

## 要素节点结构

每个图谱要素在 `NODES` 中定义静态信息：

```js
{
  id: 'tool',                         // 要素唯一 ID
  title: '工具/手段',                 // 卡片标题
  x: 620,                             // 画布 x 坐标
  y: 280,                             // 画布 y 坐标
  w: 240,                             // 节点宽度
  h: 160,                             // 节点高度
  photo: 'assets/作案工具烟灰缸.jpg',  // 可选，图片材料
  video: 'assets/证人证言.mp4'         // 可选，视频材料
}
```

当前 7 个要素：

```js
const NODES = {
  conseq: {
    id: 'conseq',
    title: '后果',
    media: 'assets/被害人伤情.JPG'
  },

  tool: {
    id: 'tool',
    title: '工具/手段',
    media: 'assets/作案工具烟灰缸.jpg'
  },

  victim: {
    id: 'victim',
    title: '受害人',
    media: 'assets/被害人人像.JPG'
  },

  suspect: {
    id: 'suspect',
    title: '嫌疑人',
    media: 'assets/嫌疑人人像.jpg'
  },

  circumstance: {
    id: 'circumstance',
    title: '情节',
    media: null
  },

  time: {
    id: 'time',
    title: '时间地点',
    media: 'assets/现场照片.jpg'
  },

  witness: {
    id: 'witness',
    title: '证人',
    media: 'assets/证人证言.mp4'
  }
}
```

## 阶段节点状态结构

每个阶段的 `STEPS[n].nodes[nodeId]` 定义节点在当前阶段的摘要状态：

```js
{
  state: 'green' | 'yellow' | 'red',  // 节点颜色状态
  status: '已证实' | '补强中',        // 状态文案
  key: '用烟灰缸殴打李江头部',        // 节点主文案
  sub: '多源证据印证一致',            // 节点副文案，当前 UI 只有 time 节点显示
  bar: ['g','g','g'],                 // 三性状态：真实性、关联性、合法性
  badges: [5,0,0]                     // [已有证据数, 缺失证据数, 矛盾数]
}
```

`badges` 和详情卡片严格对应：

```js
badges[0] === detail.evidences.length
badges[1] === detail.gaps.length
badges[2] === detail.conflicts.length
```

## 展开详情结构

每个要素展开后使用 `getNodeDetail(id, currentStep)` 读取详情：

```js
{
  evidences: EvidenceItem[],
  gaps: GapItem[],
  conflicts: ConflictItem[]
}
```

证据项结构：

```js
{
  name: '作案工具照片',       // 关联 MATERIALS 的 key，或展示名
  tag: '工具',               // 卡片标签
  desc: '透明玻璃烟灰缸已拍照固定',
  bar: ['g','g','g']          // 三性状态，当前 mini-bar 已移除但数据仍保留
}
```

缺口项结构：

```js
{
  title: '缺：周枫询问笔录',
  action: '对周枫制作询问笔录'
}
```

矛盾项结构：

```js
{
  title: '动作描述不一致',
  sideA: {
    src: '李江询问笔录',
    val: '...'
  },
  sideB: {
    src: '周枫自书',
    val: '...'
  },
  investigation: '系统分析说明',
  suggest: '建议动作',
  tasks: ['派生任务 1', '派生任务 2']
}
```

当前这版数据里，多数 `conflicts` 已为空，主要通过 `gaps` 和 `evidences` 表示补强过程。

## 材料索引结构

`MATERIALS` 是证据详情卡片和材料抽屉的统一材料索引：

```js
{
  title: '证据保全决定书',           // 材料标题
  type: '程序文书',                 // 材料类型
  file: 'assets/证据保全决定书.pdf', // 原始文件
  owner: '周枫',                    // 关联人员
  time: '2026-05-06',               // 形成时间
  source: '办案系统',               // 来源：只允许“办案系统”或“固证精灵”
  elements: ['工具/经过','程序'],    // 支撑的图谱要素
  excerpt: '材料摘要'
}
```

当前来源口径：

```js
source: '办案系统'  // 程序文书、笔录、核验、警情
source: '固证精灵'  // 照片、视频等固证材料
```

## 每个要素的数据语义

```js
{
  time: {
    title: '时间地点',
    meaning: '案发时间与案发地点',
    keyFields: ['警情时间戳', '地点GPS', '现场照片'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '2026-04-06 09:55',
      badges: [2,0,0]
    }
  },

  conseq: {
    title: '后果',
    meaning: '被害人伤害结果',
    keyFields: ['伤情照片', '体表原始伤情记录表', '医院伤情检查通知单', '伤情检查结论告知书'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '头皮挫裂伤',
      badges: [4,0,0]
    }
  },

  tool: {
    title: '工具/手段',
    meaning: '违法行为方式、工具和过程',
    keyFields: ['警情概述', '作案工具照片', '现场勘验照片', '证据保全决定书', '证据保全清单'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '用烟灰缸殴打李江头部',
      badges: [5,0,0]
    }
  },

  witness: {
    title: '证人',
    meaning: '现场目击证人及证言',
    keyFields: ['证人吴渔证言'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '吴渔·女·31岁',
      badges: [1,0,0]
    }
  },

  victim: {
    title: '受害人',
    meaning: '被害人身份、陈述和相关程序材料',
    keyFields: ['李江人像照片', '李江身份核验', '李江询问笔录', '行政案件立案告知书'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '李江·男·29岁',
      badges: [4,0,0]
    }
  },

  suspect: {
    title: '嫌疑人',
    meaning: '违法嫌疑人身份、到案、陈述、自书和处罚告知',
    keyFields: ['嫌疑人人像照片', '周枫身份核验', '传唤证', '周枫询问笔录', '周枫自书', '行政处罚告知笔录'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '周枫·男·31岁·初犯·认错认罚',
      badges: [6,0,0]
    }
  },

  circumstance: {
    title: '情节',
    meaning: '初犯、主动到案、认错认罚、快速办理等量罚情节',
    keyFields: ['前科核验', '认罪认罚材料', '快速办理权利义务告知书', '行政处罚告知笔录'],
    finalState: {
      state: 'green',
      status: '已证实',
      key: '初犯·认错认罚',
      badges: [4,0,0]
    }
  }
}
```

## 边关系结构

图谱连线由 `EDGES` 定义：

```js
{
  from: 'tool',
  to: 'conseq',
  type: 'cause',
  label: '印证'
}
```

当前边关系：

```js
[
  { from:'tool', to:'conseq', type:'cause', label:'印证' },
  { from:'conseq', to:'victim', type:'cause', label:'属于' },
  { from:'suspect', to:'tool', type:'cause', label:'使用' },
  { from:'circumstance', to:'suspect', type:'cause', label:'归属' }
]
```

## 推荐抽象后的统一数据结构

如果后续要把这份 HTML 改成接口驱动，建议把每个要素整理成这种结构：

```ts
type GraphElement = {
  id: 'time' | 'conseq' | 'tool' | 'witness' | 'victim' | 'suspect' | 'circumstance'
  title: string
  layout: {
    x: number
    y: number
    width: number
    height: number
  }
  media?: {
    type: 'image' | 'video'
    url: string
    materialName: string
  }
  stageState: {
    [step: number]: {
      state: 'green' | 'yellow' | 'red'
      status: string
      key: string
      sub: string
      bar: ['g' | 'y' | 'r', 'g' | 'y' | 'r', 'g' | 'y' | 'r']
      badges: [number, number, number]
    }
  }
  stageDetail: {
    [step: number]: {
      evidences: EvidenceItem[]
      gaps: GapItem[]
      conflicts: ConflictItem[]
    }
  }
}
```

其中材料独立成索引表：

```ts
type Material = {
  name: string
  title: string
  type: string
  file: string
  owner: string
  time: string
  source: '办案系统' | '固证精灵'
  elements: string[]
  excerpt: string
}
```
